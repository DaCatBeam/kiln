# kubernetes_node_upgrade

Defines a separate, serial upgrade boundary. The initial scaffold deliberately
fails when enabled until drain, version-skew, rollback, and uncordon behavior
has executable verification.
