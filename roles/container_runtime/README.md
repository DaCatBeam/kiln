# container_runtime

Installs and starts a configurable runtime package, defaulting to `containerd`.
Repository setup and hardened runtime configuration are intentionally future
work, so the role is disabled by default.
