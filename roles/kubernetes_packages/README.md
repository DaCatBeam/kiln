# kubernetes_packages

Installs `kubelet`, `kubeadm`, and `kubectl` after a trusted package repository
has been configured. Repository key and pinning work is intentionally not
implied by this skeleton, and the role remains disabled by default.
