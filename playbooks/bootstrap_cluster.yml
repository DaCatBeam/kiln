---
- name: Bootstrap Kubernetes control plane nodes
  hosts: control_plane
  become: true
  gather_facts: true
  serial: 1
  roles:
    - role: kubeadm_control_plane

- name: Join Kubernetes worker nodes
  hosts: workers
  become: true
  gather_facts: true
  roles:
    - role: kubeadm_worker
